# TIFF Coverage Audit Summary (2026-05-04)

Generated by `tools/audit_corpus.sh --default`. Source matrix:
`audit/coverage_matrix.tsv` (18 files).

## Corpus

| Source | Files |
|---|---|
| `~/Documents-CloudManaged/validate/ground_truth_examples/tiff/`  | 15 |
| `~/Documents-CloudManaged/validate/ground_truth_examples/dng/`   | 2  |
| `/Volumes/Fileserver/Pictures/scan from pete's book.tif`         | 1  |
| **Total**                                                        | 18 |

## Variant coverage observed

### Compressions
| Compression           | Files | M-target |
|-----------------------|-------|----------|
| None (uncompressed)   | 4     | M3       |
| PackBits              | 2     | M4       |
| LZW                   | 7     | M4       |
| AdobeDeflate          | 1     | M4       |
| CCITT Group 3 (T.4)   | 1     | M4       |
| CCITT Group 4 (T.6)   | 1 (the marquee reproducer) | M4 |
| JPEG (in DNG)         | 2     | M9.5 (deferred) |
| LERC, ZSTD-in-TIFF, JPEG2000 | **0** — needs gen | M12 |

### Photometric interpretations
| Photometric                       | Files |
|-----------------------------------|-------|
| min-is-white (fax/scan)           | 4     |
| min-is-black (grayscale)          | 3     |
| RGB color                         | 5     |
| Palette (RGB from colormap)       | 2     |
| YCbCr                             | 1     |
| 32803 / 0x8023 (CFA Bayer, DNG)   | 2     |
| CMYK / separated                  | **0** — needs gen |
| CIE L\*a\*b\*                     | **0** — needs gen |

### Bit depths
| Bits/Sample | Files |
|-------------|-------|
| 1           | 3     |
| 8           | 13    |
| 12          | 1 (DNG) |
| 16          | 1 (DNG) |
| 32 float    | **0** — needs gen for predictor 3 (DNG HDR) |

### Layout
| Layout    | Files |
|-----------|-------|
| Stripped  | 15    |
| Tiled     | 3 (`cramps-tile`, `quad-tile`, `blackmagic_micro_cinema`) |

### Multi-IFD / multi-page
- `pc260001.tif`: 2 IFDs (image + EXIF sub-IFD).
- `L1006922_leica_M11.DNG`: 7 IFDs (main raw + previews + embedded thumbnails — this is the standard DNG layout).
- All others: single IFD.

### Other axes
- **BigTIFF**: 0 files. **Needs acquired or generated fixture.**
- **Predictor**: only `deflate-last-strip.tiff` declares one (predictor=1 / none). Need generated fixtures for predictor=2 (Horizontal) and predictor=3 (Floating-point). M5 territory.
- **GeoTIFF**: 0 files. M11 territory; generate via `gdal_translate -a_srs EPSG:4326`.
- **ICC profile present**: 0 files. Most pro-photography TIFFs ship one — gap in the corpus.

## Marquee test case

`/Volumes/Fileserver/Pictures/scan from pete's book.tif` — 1.3 MB,
big-endian, 11059×15671, 1-bit, photometric=min-is-white,
compression=CCITT Group 4, single strip (RowsPerStrip=15671), no
predictor, no ICC, no EXIF. **This is the file where `pmarreck/zigimg`
PR #321's pair-step T.6 decoder drifts after row 1030.** Marquee
target for M4-T.6.

## zigimg verdict (qualitative — to be measured precisely)

`tiffinfo` parsing succeeds on every file in the corpus (good — these
are well-formed TIFFs). Predicting zigimg's behavior from variant axes:

| Verdict      | Files | Reason |
|--------------|-------|--------|
| OK           | 14    | None / PackBits / LZW / Deflate / RGB / palette / min-is-{white,black} / CCITT G3 |
| Structural drift | 1 | The CCITT G4 marquee scan (after row 1030) |
| Error / WARN | 3     | YCbCr photometric + 2× DNG-with-JPEG-Bayer (compression=7 inside compression=34892 layout) |

To turn this into precise per-file outcomes, instrument validate to
log its TIFF deep-validation verdict per file and append the column to
the matrix. Deferred — the variant axes alone are enough to scope
tiffz milestones.

## Gaps that drive future fixture generation

The corpus does not exercise the following — they get generated under
SPEC §B recipes during milestone work that touches each axis:

- **BigTIFF** (M7): `tiffcp -8 ...`
- **Predictor 2 horizontal** (M5): `tiffcp -c lzw -p 2 ...`
- **Predictor 3 floating-point** (M5): `gdal_translate -co PREDICTOR=3 ...`
- **CMYK / Lab photometrics** (M9): `magick -colorspace CMYK / Lab ...`
- **GeoTIFF** (M11): `gdal_translate -a_srs EPSG:... ...`
- **LERC / ZSTD-in-TIFF** (M12): `gdal_translate -co COMPRESS=LERC / ZSTD ...`
- **JPEG2000-in-TIFF** (M12, optional): rare, defer unless pro-photo blocked.

## Implications for milestone planning

The audit confirms SPEC §3's coverage hypothesis. Concrete adjustments:

1. **M4 sequencing is right.** Real-world coverage benefits from
   PackBits → LZW → Deflate → CCITT G3 → CCITT G4 in that order; the
   marquee target falls at the end (G4) which is correct.
2. **M5 (predictors) needs fixtures, not corpus** — the audit doesn't
   include predictor=2/3 files. Generate during M5.
3. **M6 (tile-based) has 3 corpus fixtures already** — `cramps-tile`,
   `quad-tile`, `blackmagic_micro_cinema`. Good organic coverage.
4. **DNG (M8) is exercised by 2 real files** with both layouts
   (stripped + tiled) and both bit depths (12 + 16). Solid baseline
   even before generating synthetic DNG fixtures.
5. **YCbCr (M9) has one corpus fixture** — `ycbcr-cat.tif`. Generate
   matching fixtures for CMYK and Lab.
6. **No BigTIFF in corpus** — acquire or generate one before M7.
   `tiffcp -8 bali.tif test_bigtiff.tif` is the simplest path.

## Reproducing this audit

```bash
nix develop -c tools/audit_corpus.sh --default > audit/coverage_matrix.tsv
```

Script supports passing arbitrary file/dir paths. The default set is
the three locations above.
